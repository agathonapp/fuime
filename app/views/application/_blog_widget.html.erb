<%# FUIME-DISABLED: the "What's new?" changelog widget.

    Upstream this rendered in the header of every authenticated page, plus the
    admin layout, the user menu, and the docs header. All three of its parts
    pointed at Hack Club:

      - the icon linked to blog.hcb.hackclub.com, under the label
        "See what's new with Fuime"
      - the dropdown embedded blog.hcb.hackclub.com/embed in an iframe, so
        opening it loaded a document from Hack Club's server
      - blog_controller.js fetched blog.hcb.hackclub.com/api/unreads with
        `credentials: 'include'` on every page load, to draw an unread badge

    That is a live dependency on another organization's infrastructure across
    Fuime's most-visited surface, and the content is HCB's changelog rather
    than Fuime's (Prime Directive 4).

    Rendering nothing until Fuime has a changelog of its own. The four render
    sites are left in place so restoring it means editing this file alone; the
    Stimulus controller is kept for the same reason. %>
