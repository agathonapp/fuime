<%# Fuime: RETIRED 2026-09-14 — there is no paywall to put signage in front of.

    This was the free plan's one-venture wall, shown where the teen was rather
    than where the admin was, so nobody discovered the limit inside
    Event::Application#activate_event!. Both are gone: the blocker was removed
    with the family plan, and Fuime is one flat price with unlimited businesses
    included.

    The partial is kept and rendered as nothing, rather than deleted along with
    its `render` call, per Rule 2 — and because an empty partial is a much
    smaller thing to find and fill than a render site to re-derive if a limit
    ever comes back. It has one job when it does: say what the limit is BEFORE a
    founder hits it. %>
