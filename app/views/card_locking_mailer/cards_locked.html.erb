<p>
  Your Fuime cards are locked because <%= @count %> <%= "receipt".pluralize(@count) %> <%= @count == 1 ? "is" : "are" %> overdue. Upload <%= @count == 1 ? "it" : "them" %> and your cards work again in seconds.
</p>
<p>
  Every charge needs a receipt within 7 days. Manage your receipts on the Fuime <%= link_to "receipts page", my_inbox_url %>.
</p>
<p>
  Any subscriptions or recurring charges on this card will also be declined until you upload.
</p>
<%= render "transactions_table", hcb_codes: @hcb_codes, show_org: @show_org %>
