<footer data-behavior="hide_iframe">
  <%# Fuime 2026-09-13: this footer asserted, on a PUBLIC page
      (`/donate/start/:event_name` is a plain GET), that the venture "is fiscally
      sponsored by Hack Club, a 501(c)(3) nonprofit. Our registered EIN is
      81-2908499" — with the EIN written as Fuime's own. Both halves are false
      about a Fuime venture, and the second claims another organisation's tax ID.
      BRAND_STRINGS.md category 3: there is no true Fuime version of that
      sentence, so it is replaced by the standing disclosure rather than
      reworded. %>
  <%= render "application/status_disclosure" %>
  <p class="muted h5 center">
    Questions? Get in touch at <%= help_email %>.
  </p>
</footer>
