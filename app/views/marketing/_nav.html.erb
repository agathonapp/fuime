<%# Shared marketing header. Auth-aware (rendered per-request). Reused by all
    /for/* landing pages — keep it page-agnostic. %>
<header class="marketing-nav">
  <div class="marketing-nav__inner">
    <% logo_env = Rails.env.local? ? "development" : Rails.env %>
    <a href="<%= root_path %>" class="marketing-nav__logo" aria-label="Fuime home">
      <%= image_tag "logo-#{logo_env}.svg", alt: "Fuime", height: 30 %>
    </a>

    <nav class="marketing-nav__links" aria-label="Primary">
      <%# Was a link to hackclub.com/fiscal-sponsorship — Hack Club's product,
          not Fuime's (CLAUDE.md Rule 7). %>
      <a href="<%= faq_path %>">How it works</a>
      <a href="<%= funders_path %>" aria-current="page">For funders</a>
    </nav>

    <div class="marketing-nav__actions">
      <% if signed_in? %>
        <a href="<%= root_path %>" class="marketing-nav__dashboard">
          <%= avatar_for current_user, size: 26 %>
          <span>Dashboard</span>
          <span class="marketing-nav__arrow" aria-hidden="true">&rarr;</span>
        </a>
      <% else %>
        <a href="<%= auth_users_path %>" class="marketing-nav__login">Log in</a>
        <a href="#talk-to-us" class="marketing-btn marketing-btn--sm marketing-btn--primary">Talk to our team</a>
      <% end %>
    </div>
  </div>
</header>
