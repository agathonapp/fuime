development:
  adapter: async

test:
  adapter: test

production:
  adapter: redis
  url: <%= ENV["REDIS_CACHE_URL"].presence || ENV["REDIS_URL"].presence || "redis://localhost:6379/1" %>
  ssl_params:
    verify_mode: <%= OpenSSL::SSL::VERIFY_NONE %>
  channel_prefix: bank_production
