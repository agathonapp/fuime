{
  "name": "fuime-site",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "fuime landing page + waitlist capture",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server.js",
    "test": "node tools/sync-chrome.mjs --check && node test/copy-guard.mjs && node test/waitlist.test.mjs && node test/server.test.mjs",
    "chrome": "node tools/sync-chrome.mjs"
  },
  "dependencies": {
    "ioredis": "^6.0.0"
  }
}
