{
  "name": "fuime-site",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "fuime landing page + waitlist capture",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server.js",
    "test": "node test/waitlist.test.mjs && node test/server.test.mjs"
  }
}
