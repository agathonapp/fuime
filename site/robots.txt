# fuime — invoicing for people who aren't 18 yet
# One page while the marketing site is closed: / is the front door and the rest
# 307 to it. Deliberately no Disallow on /home, /pricing or /parents — a
# crawler that cannot fetch them never sees the redirect, and the URL sits in
# the index as a bare link instead of consolidating onto /.

User-agent: *
Allow: /

# The waitlist endpoint is a POST handler. There is nothing to read there and a
# crawler hitting it just burns a function invocation.
Disallow: /api/

Sitemap: https://fuime.com/sitemap.xml
