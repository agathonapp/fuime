# Everything here is regenerable from the scripts and the keys, and all of it is
# huge: raw/ and comp/ are 90MB of PNG apiece, shots/ is the verify walk.
# What the repo keeps is the recipe (jobs, track, composite, build) plus keys/,
# which holds the seed image and the plates the whole dive is built from.
raw/
comp/
frames/
shots/
master.mp4
